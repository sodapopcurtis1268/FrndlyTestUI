import { createRowTest } from '../../utils/createRowTest';

// Personalised row — content varies by account and may include VOD (DRM).
createRowTest('Recommended for You', { skipOnTimeout: true }, 420681);
